%li= link_to 'Logout', destroy_admin_session_path, :method => :delete

.row
  .small-12.columns
    - @posts.each do |post|
      .post
        = "Title: #{ post.title }"
        = post&.body&.html_safe